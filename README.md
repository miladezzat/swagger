# swagger

```
    firs install swagger:
        sudo npm install -g swagger
    and
    git clone https://github.com/miladezzat/swagger.git
    cd swagger
    npm i --save
    swagger project start
    and open new compand line and write: swagger project edit
```