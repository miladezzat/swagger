# swagger

```
    git clone https://github.com/miladezzat/swagger.git
    cd swagger
    npm i --save
    npm start
    
    for testing:
    npm run test

    for seeing swagger edit
    install swagger first and write the following commands:
    swagger project start
    swagger project edit
```